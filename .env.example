# Sawa Admin — environment template. Copy to .env.local and fill in.
# The admin panel talks to the Sawa server's admin API.

# Base URL of the Sawa server API (no trailing slash), e.g. https://your-server.example.com/api/v1
NEXT_PUBLIC_API_URL=
